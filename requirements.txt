pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
mlflow==2.13.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
joblib==1.4.2
evidently==0.4.25
python-multipart==0.0.9
anthropic==0.40.0
groq==0.11.0
# Pinned: httpx>=0.28 removed the `proxies` kwarg that anthropic==0.40.0 and
# groq==0.11.0's internal HTTP client construction still pass, which breaks
# client init with "Client.__init__() got an unexpected keyword argument
# 'proxies'". Keep below 0.28 until the SDKs above are upgraded.
httpx==0.27.2
